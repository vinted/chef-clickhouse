<yandex>
    <zookeeper>
        <% @nodes.each_with_index do |no, i| %>
          <node index="<%= no[:index] || i %>">
              <host><%= no[:host] %></host>
              <port><%= no[:port] %></port>
          </node>
        <% end %>
    </zookeeper>
</yandex>
